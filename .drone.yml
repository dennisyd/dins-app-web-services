kind: pipeline
name: default

steps:
  - name: test
    image: docker/compose:1.24
    volumes:
      - name: docker_sock
        path: /var/run/docker.sock
    commands:
      - up -f /drone/src/docker-compose.yml
volumes:
  - name: docker_sock
    host:
      path: /var/run/docker.sock
